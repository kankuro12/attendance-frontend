import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'np.com.needtech.attendance',
  appName: 'attendance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
